<%= render "shared/navbar" %>

<div class="activities-container">
  <div class="activities-box">
  <p>Choose three activities</p>
    <div id="card-activities-all">
      <%= render "trips/activities", trip: @trip, activities_one: @activities_one, activities_two: @activities_two, activities_three: @activities_three, activity_one: @activity_one, activity_two: @activity_two, activity_three: @activity_three %>
    </div>
  </div>
  <div class="submit-edit-trip">
      <%= link_to "Save & Send Later", trip_path(@trip), id:"first-btn", class: @trip.visits.length == 3 ? "" : "btn-secondary", method:"patch" %>
      <% if @trip.visits.length == 3 %>
        <%= link_to "Send to Friend", "#modal-comment", id:"last-btn", class:"btn-secondary", 'data-toggle'=> "modal", 'data-target'=> "#modal-comment" %>
      <% else %>
        <%= link_to "Send to Friend", "#modal-comment", style:"display:none", id:"last-btn", class:"btn-secondary", 'data-toggle'=> "modal", 'data-target'=> "#modal-comment" %>
        <%= render 'shared/modal_comment', trip: @trip %>
      <% end %>
  </div>
</div>


